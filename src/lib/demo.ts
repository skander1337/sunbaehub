/** Demo-only passwords for the seeded accounts. Used by the seed script; never rendered in the UI. */
export const DEMO_PASSWORDS = { seeker: "hoobae1234", expert: "sunbae1234", admin: "admin1234" } as const;
